import { LzbBlurFocusDirective } from './lzb-blur-focus.directive';

describe('LzbBlurFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new LzbBlurFocusDirective();
    expect(directive).toBeTruthy();
  });
});
